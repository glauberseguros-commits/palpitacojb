===== LKN-44 — VALIDAÇÃO DO CALENDÁRIO TOP3 =====

ALTERAÇÕES IMPLEMENTADAS:
- Federal histórica: quarta e sábado às 20h.
- Federal desde 19/07/2026: quarta às 20h e domingo às 11h.
- PT Rio domingo desde 18/07/2026: sem 09h e 11h.
- PT Rio sábado desde 18/07/2026: 18h não é mais removido pela Federal.
- Histórico anterior preservado.

VALIDAÇÕES TEXTUAIS:
- vigencia_federal_domingo: OK
- vigencia_rio_domingo: OK
- preserva_regra_2025: OK
- federal_domingo_11h: OK
- restaura_rio_18h_sabado: OK

==========================================================================================
COMANDO: git diff --check
EXIT CODE: 0

STDERR:
warning: in the working copy of 'backend/scripts/backtestTop3Official.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/pages/Top3/Top3View.jsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/pages/Top3/top3.engine.js', LF will be replaced by CRLF the next time Git touches it

==========================================================================================
COMANDO: git diff -- src/pages/Top3/top3.engine.js
EXIT CODE: 0

STDOUT:
diff --git a/src/pages/Top3/top3.engine.js b/src/pages/Top3/top3.engine.js
index 40c0b25..896d3ce 100644
--- a/src/pages/Top3/top3.engine.js
+++ b/src/pages/Top3/top3.engine.js
@@ -213,43 +213,117 @@ function ymdHourToTs(ymd, hourBucket) {
    Schedules
 ========================= */
 
+const FEDERAL_SUNDAY_START_YMD = "2026-07-19";
+const PT_RIO_SUNDAY_REDUCED_START_YMD = "2026-07-18";
+const FEDERAL_20_REMOVES_PT_RIO_18_START_YMD = "2025-11-03";
+
+/**
+ * Calendário Federal com preservação histórica.
+ *
+ * Até 18/07/2026:
+ * - quarta-feira às 20h;
+ * - sábado às 20h.
+ *
+ * A partir de 19/07/2026:
+ * - quarta-feira às 20h;
+ * - domingo às 11h;
+ * - sábado deixa de ter sorteio Federal.
+ */
 export function isFederalDrawDay(ymd) {
-  const dow = getDowKey(ymd);
+  const y = safeStr(ymd);
+  if (!y) return false;
+
+  const dow = Number(getDowKey(y));
+
+  if (y >= FEDERAL_SUNDAY_START_YMD) {
+    return dow === 0 || dow === 3;
+  }
+
   return dow === 3 || dow === 6;
 }
 
+function getFederalScheduleForYmd(ymd, FEDERAL_SCHEDULE) {
+  const y = safeStr(ymd);
+  if (!y || !isFederalDrawDay(y)) return [];
+
+  const dow = Number(getDowKey(y));
+
+  if (y >= FEDERAL_SUNDAY_START_YMD && dow === 0) {
+    return ["11:00"];
+  }
+
+  const historical = Array.isArray(FEDERAL_SCHEDULE)
+    ? FEDERAL_SCHEDULE
+    : [];
+
+  const normalized = historical
+    .map(toHourBucket)
+    .filter(Boolean);
+
+  if (normalized.includes("20:00")) {
+    return ["20:00"];
+  }
+
+  return normalized.length ? normalized : ["20:00"];
+}
+
+/**
+ * Calendário PT Rio com vigência por data.
+ *
+ * Nenhum horário histórico é removido dos dados.
+ * Esta função determina apenas os slots esperados
+ * para cada data.
+ */
 export function getPtRioScheduleForYmd(
   ymd,
   PT_RIO_SCHEDULE_NORMAL,
   PT_RIO_SCHEDULE_WED_SAT
 ) {
-  const dow = getDowKey(ymd);
+  const y = safeStr(ymd);
+  const dow = Number(getDowKey(y));
 
-  if (dow === 0) return PT_RIO_SCHEDULE_SUNDAY;
+  if (dow === 0) {
+    const sundaySchedule = Array.isArray(PT_RIO_SCHEDULE_SUNDAY)
+      ? [...PT_RIO_SCHEDULE_SUNDAY]
+      : [];
+
+    if (y >= PT_RIO_SUNDAY_REDUCED_START_YMD) {
+      return sundaySchedule.filter((hour) => {
+        const h = toHourBucket(hour);
+        return h !== "09:00" && h !== "11:00";
+      });
+    }
+
+    return sundaySchedule;
+  }
 
   if (dow === 3 || dow === 6) {
     const baseSchedule = Array.isArray(PT_RIO_SCHEDULE_WED_SAT)
-      ? PT_RIO_SCHEDULE_WED_SAT
+      ? [...PT_RIO_SCHEDULE_WED_SAT]
       : [];
 
-    // A Federal mudou de 19h para 20h em 03/11/2025.
-    // Desde então, nas quartas e sábados, o PT Rio das 18h
-    // é substituído pelo sorteio das 19h.
-    if (String(ymd || "").trim() >= "2025-11-03") {
-      return Array.from(
-        new Set([
-          ...baseSchedule
-            .map(toHourBucket)
-            .filter((hour) => hour && hour !== "18:00"),
-          "19:00",
-        ])
-      ).sort((a, b) => hourToInt(a) - hourToInt(b));
+    const federalRemovedRio18 =
+      y >= FEDERAL_20_REMOVES_PT_RIO_18_START_YMD &&
+      (
+        dow === 3 ||
+        (
+          dow === 6 &&
+          y < PT_RIO_SUNDAY_REDUCED_START_YMD
+        )
+      );
+
+    if (federalRemovedRio18) {
+      return baseSchedule.filter(
+        (hour) => toHourBucket(hour) !== "18:00"
+      );
     }
 
     return baseSchedule;
   }
 
-  return PT_RIO_SCHEDULE_NORMAL;
+  return Array.isArray(PT_RIO_SCHEDULE_NORMAL)
+    ? [...PT_RIO_SCHEDULE_NORMAL]
+    : [];
 }
 
 export function getScheduleForLottery({
@@ -262,7 +336,10 @@ export function getScheduleForLottery({
   const key = safeStr(lotteryKey).toUpperCase();
 
   if (key === "FEDERAL") {
-    return isFederalDrawDay(ymd) ? FEDERAL_SCHEDULE : [];
+    return getFederalScheduleForYmd(
+      ymd,
+      FEDERAL_SCHEDULE
+    );
   }
 
   return getPtRioScheduleForYmd(
STDERR:
warning: in the working copy of 'src/pages/Top3/top3.engine.js', LF will be replaced by CRLF the next time Git touches it
