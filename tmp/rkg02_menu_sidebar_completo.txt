

==============================================================================================================
ARQUIVO: .\src\pages\Dashboard\components\Sidebar\AppShell.jsx
==============================================================================================================
import React, { useEffect, useMemo, useState } from "react";
import Icon from "./Icon";
import MiniLogo from "./MiniLogo";

import { auth } from "../../../../services/firebase";
import { onAuthStateChanged, signOut } from "firebase/auth";

const ROUTES = {
  DASHBOARD: "dashboard",
  ACCOUNT: "account",
  RESULTS: "results",
  TOP3: "top3",
  LATE: "late",
  SEARCH: "search",
  PAYMENTS: "payments",
  DOWNLOADS: "downloads",
  CENTENAS: "centenas",
};

const ACCOUNT_SESSION_KEY = "pp_session_v1";
const LS_GUEST_ACTIVE_KEY = "pp_guest_active_v1";

const safeReadLS = (k) => {
  try {
    return localStorage.getItem(k);
  } catch {
    return null;
  }
};

const safeRemoveLS = (k) => {
  try {
    localStorage.removeItem(k);
  } catch {}
};

function normalizePlan(plan) {
  const p = String(plan || "").trim().toUpperCase();
  if (p === "VIP") return "VIP";
  if (p === "PREMIUM") return "PREMIUM";
  if (p === "FREE") return "FREE";
  return "";
}

function readSession() {
  const raw = safeReadLS(ACCOUNT_SESSION_KEY);
  if (!raw) {
    return {
      ok: false,
      type: "anon",
      plan: "",
      uid: "",
      email: "",
      raw: null,
    };
  }

  try {
    const obj = JSON.parse(raw);
    const type = String(obj?.type || "").trim().toLowerCase();
    const plan = normalizePlan(obj?.plan);

    return {
      ok: obj?.ok === true,
      type: type || "anon",
      plan: type === "guest" ? "FREE" : plan || (type === "user" ? "FREE" : ""),
      uid: String(obj?.uid || "").trim(),
      email: String(obj?.email || "").trim().toLowerCase(),
      raw: obj,
    };
  } catch {
    return {
      ok: true,
      type: "user",
      plan: "FREE",
      uid: "",
      email: "",
      raw: null,
    };
  }
}

function useViewport() {
  const [vw, setVw] = useState(() =>
    typeof window !== "undefined" ? window.innerWidth : 1200
  );

  useEffect(() => {
    const onR = () => setVw(window.innerWidth);
    window.addEventListener("resize", onR);
    return () => window.removeEventListener("resize", onR);
  }, []);

  return vw;
}

export default function AppShell({ active, onNavigate, onLogout, children }) {
  const [session, setSession] = useState(() => readSession());
  const vw = useViewport();
  const isMobile = vw < 980;

  const [sidebarOpen, setSidebarOpen] = useState(() => {
    if (typeof window === "undefined") return true;
    return window.innerWidth >= 980;
  });

  useEffect(() => {
    if (!isMobile) setSidebarOpen(true);
    else setSidebarOpen(false);
  }, [isMobile]);

  useEffect(() => {
    const unsub = onAuthStateChanged(auth, (user) => {
      if (user?.uid) {
        setSession(readSession());
        return;
      }

      if (safeReadLS(LS_GUEST_ACTIVE_KEY) === "1") {
        setSession({
          ok: true,
          type: "guest",
          plan: "FREE",
          uid: "",
          email: "",
          raw: null,
        });
        return;
      }

      setSession({
        ok: false,
        type: "anon",
        plan: "",
        uid: "",
        email: "",
        raw: null,
      });
    });

    return () => unsub();
  }, []);

  useEffect(() => {
    const refresh = () => setSession(readSession());

    const onStorage = (e) => {
      if (!e) return;
      if (e.key === ACCOUNT_SESSION_KEY || e.key === LS_GUEST_ACTIVE_KEY) {
        refresh();
      }
    };

    window.addEventListener("storage", onStorage);
    window.addEventListener("pp_session_changed", refresh);
    window.addEventListener("focus", refresh);

    return () => {
      window.removeEventListener("storage", onStorage);
      window.removeEventListener("pp_session_changed", refresh);
      window.removeEventListener("focus", refresh);
    };
  }, []);

  const isGuest = session?.type === "guest";
  const planLabel = isGuest ? "PREVIEW" : normalizePlan(session?.plan) || "FREE";

  useEffect(() => {
    const isDashboard = active === ROUTES.DASHBOARD;

    const prevHtmlOverflow = document?.documentElement?.style?.overflow;
    const prevBodyOverflow = document?.body?.style?.overflow;

    const mustLock = (!isMobile && isDashboard) || (isMobile && sidebarOpen);

    if (mustLock) {
      document.documentElement.style.overflow = "hidden";
      document.body.style.overflow = "hidden";
    } else {
      document.documentElement.style.overflow = prevHtmlOverflow || "";
      document.body.style.overflow = prevBodyOverflow || "";
    }

    return () => {
      document.documentElement.style.overflow = prevHtmlOverflow || "";
      document.body.style.overflow = prevBodyOverflow || "";
    };
  }, [active, isMobile, sidebarOpen]);

  useEffect(() => {
    if (!isMobile) return;

    const onKeyDown = (e) => {
      if (e.key === "Escape") setSidebarOpen(false);
    };

    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [isMobile]);

  const handleNavigate = async (key) => {
    if (!key) return;

    if (isMobile) setSidebarOpen(false);

    if (key === "__LOGOUT__") {
      try {
        await signOut(auth);
      } catch {}

      safeRemoveLS(ACCOUNT_SESSION_KEY);
      safeRemoveLS(LS_GUEST_ACTIVE_KEY);

      onLogout?.();
      return;
    }

    onNavigate?.(key);
  };

  const UI = useMemo(() => {
    const GOLD = "rgba(202,166,75,1)";
    const GOLD_SOFT = "rgba(202,166,75,0.18)";
    const GOLD_SOFT_2 = "rgba(202,166,75,0.10)";
    const WHITE = "rgba(255,255,255,0.92)";
    const MUTED = "rgba(255,255,255,0.58)";
    const BORDER = "rgba(255,255,255,0.10)";
    const BG = "#050505";

    const SAFE_TOP = "env(safe-area-inset-top, 0px)";

    const sidebarW = isMobile ? 88 : 96;
    const isDashboard = active === ROUTES.DASHBOARD;

    const shell = {
      minHeight: "100dvh",
      maxHeight: "100dvh",
      height: "100dvh",
      background: BG,
      position: "relative",
      overflow: "hidden",
      display: "flex",
      flexDirection: "row",
    };

    const sidebarBase = {
      width: sidebarW,
      minWidth: sidebarW,
      height: "100%",
      borderRight: `1px solid ${BORDER}`,
      background:
        "linear-gradient(180deg, rgba(12,12,12,0.98) 0%, rgba(8,8,8,0.98) 52%, rgba(5,5,5,1) 100%)",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 0,
      padding: "12px 10px 14px",
      boxSizing: "border-box",
    };

    const sidebar = isMobile
      ? {
          ...sidebarBase,
          position: "fixed",
          top: 0,
          left: 0,
          bottom: 0,
          zIndex: 60,
          transform: sidebarOpen ? "translateX(0)" : "translateX(-110%)",
          transition: "transform 220ms ease",
          boxShadow: "18px 0 48px rgba(0,0,0,0.65)",
        }
      : {
          ...sidebarBase,
          position: "relative",
          zIndex: 2,
        };

    const overlay = isMobile
      ? {
          position: "fixed",
          inset: 0,
          zIndex: 50,
          background: "rgba(0,0,0,0.55)",
          backdropFilter: "blur(6px)",
          WebkitBackdropFilter: "blur(6px)",
          opacity: sidebarOpen ? 1 : 0,
          pointerEvents: sidebarOpen ? "auto" : "none",
          transition: "opacity 200ms ease",
        }
      : { display: "none" };

    const brand = {
      width: "100%",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 12,
      paddingTop: 8,
      paddingBottom: 14,
      marginBottom: 10,
      borderBottom: `1px solid ${BORDER}`,
    };

    const logoWrap = {
      width: "100%",
      minHeight: 62,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
    };

    const plan = {
      minWidth: 64,
      textAlign: "center",
      fontSize: 11,
      fontWeight: 900,
      padding: "8px 12px",
      borderRadius: 999,
      border: `1px solid ${GOLD_SOFT}`,
      color: GOLD,
      background:
        "linear-gradient(180deg, rgba(202,166,75,0.10), rgba(0,0,0,0.22))",
      letterSpacing: 0.4,
      boxShadow: "inset 0 0 0 1px rgba(255,255,255,0.03)",
    };

    const nav = {
      width: "100%",
      display: "flex",
      flexDirection: "column",
      gap: 8,
      paddingTop: 2,
      alignItems: "center",
      overflow: "auto",
      flex: 1,
      WebkitOverflowScrolling: "touch",
    };

    const navDivider = {
      width: "100%",
      height: 1,
      minHeight: 1,
      margin: "6px 0 4px",
      background:
        "linear-gradient(90deg, rgba(255,255,255,0.00) 0%, rgba(255,255,255,0.10) 18%, rgba(202,166,75,0.18) 50%, rgba(255,255,255,0.10) 82%, rgba(255,255,255,0.00) 100%)",
      borderRadius: 999,
    };

    const btn = (isActive) => ({
      position: "relative",
      width: "100%",
      height: 46,
      borderRadius: 14,
      border: `1px solid ${isActive ? "rgba(202,166,75,0.55)" : BORDER}`,
      background: isActive
        ? "linear-gradient(180deg, rgba(202,166,75,0.16), rgba(0,0,0,0.38))"
        : "linear-gradient(180deg, rgba(255,255,255,0.03), rgba(0,0,0,0.22))",
      color: WHITE,
      cursor: "pointer",
      display: "grid",
      placeItems: "center",
      boxShadow: isActive
        ? "0 14px 36px rgba(0,0,0,0.55), inset 3px 0 0 rgba(202,166,75,0.95)"
        : "inset 0 0 0 1px rgba(255,255,255,0.015)",
      transition:
        "transform 160ms ease, border-color 160ms ease, background 160ms ease, box-shadow 160ms ease",
      outline: "none",
    });

    const btnMarker = (isActive) => ({
      position: "absolute",
      left: 0,
      top: 8,
      bottom: 8,
      width: 3,
      borderRadius: "0 999px 999px 0",
      background: isActive ? GOLD : "transparent",
      boxShadow: isActive ? `0 0 12px ${GOLD_SOFT}` : "none",
    });

    const btnHoverHint = {
      position: "absolute",
      inset: 0,
      borderRadius: 14,
      boxShadow: `inset 0 0 0 1px ${GOLD_SOFT_2}`,
      pointerEvents: "none",
    };

    const footerHint = {
      marginTop: "auto",
      paddingTop: 10,
      width: "100%",
      display: "flex",
      justifyContent: "center",
      color: MUTED,
      fontSize: 10,
    };

    const main = {
      flex: 1,
      minWidth: 0,
      height: "100%",
      overflow: !isMobile && isDashboard ? "hidden" : "auto",
      WebkitOverflowScrolling: "touch",
      position: "relative",
      zIndex: 1,
    };

    const mobileTopBar = isMobile
      ? {
          position: "sticky",
          top: 0,
          zIndex: 40,
          padding: `calc(${SAFE_TOP} + 10px) 0 8px 10px`,
          background:
            "linear-gradient(180deg, rgba(0,0,0,0.72), rgba(0,0,0,0.22), rgba(0,0,0,0))",
          backdropFilter: "blur(10px)",
          WebkitBackdropFilter: "blur(10px)",
        }
      : { display: "none" };

    const mobileMenuBtn = isMobile
      ? {
          width: 48,
          height: 48,
          borderRadius: 14,
          border: `1px solid ${BORDER}`,
          background:
            "linear-gradient(180deg, rgba(255,255,255,0.08), rgba(0,0,0,0.38))",
          boxShadow: "0 14px 36px rgba(0,0,0,0.55)",
          display: "grid",
          placeItems: "center",
          cursor: "pointer",
        }
      : { display: "none" };

    const mainInner =
      !isMobile && isDashboard
        ? { minWidth: 0, height: "100%", overflow: "hidden" }
        : { minWidth: 0 };

    return {
      shell,
      sidebar,
      overlay,
      brand,
      logoWrap,
      plan,
      nav,
      navDivider,
      btn,
      btnMarker,
      btnHoverHint,
      footerHint,
      main,
      mobileTopBar,
      mobileMenuBtn,
      mainInner,
    };
  }, [isMobile, sidebarOpen, active]);

  const menuPrimary = [
    { key: ROUTES.DASHBOARD, icon: "home", title: "Dashboard" },
    { key: ROUTES.RESULTS, icon: "calendar", title: "Resultados" },
    { key: ROUTES.TOP3, icon: "trophy", title: "Top 3" },
    { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
  ];

  const menuTools = [
    { key: ROUTES.SEARCH, icon: "search", title: "Busca" },
    { key: ROUTES.CENTENAS, icon: "hash", title: "Centenas" },
    { key: ROUTES.DOWNLOADS, icon: "download", title: "Downloads" },
  ];

  const menuAccount = [
    { key: ROUTES.ACCOUNT, icon: "user", title: "Minha Conta" },
  ];

  const renderMenuButton = (item) => {
    const isActive = active === item.key;

    return (
      <button
        key={item.key}
        onClick={() => handleNavigate(item.key)}
        title={item.title}
        style={UI.btn(isActive)}
        aria-current={isActive ? "page" : undefined}
        aria-label={item.title}
        type="button"
        onMouseEnter={(e) => {
          if (!isActive) {
            e.currentTarget.style.borderColor = "rgba(202,166,75,0.28)";
            e.currentTarget.style.transform = "translateY(-1px)";
            e.currentTarget.style.boxShadow =
              "0 10px 24px rgba(0,0,0,0.34), inset 0 0 0 1px rgba(202,166,75,0.08)";
          }
        }}
        onMouseLeave={(e) => {
          if (!isActive) {
            e.currentTarget.style.borderColor = "rgba(255,255,255,0.10)";
            e.currentTarget.style.transform = "translateY(0)";
            e.currentTarget.style.boxShadow =
              "inset 0 0 0 1px rgba(255,255,255,0.015)";
          }
        }}
      >
        <span style={UI.btnMarker(isActive)} />
        <span style={UI.btnHoverHint} />
        <Icon name={item.icon} size={22} />
      </button>
    );
  };

  return (
    <div style={UI.shell}>
      <div
        style={UI.overlay}
        onClick={() => setSidebarOpen(false)}
        aria-hidden="true"
      />

      <aside style={UI.sidebar} aria-label="Menu lateral">
        <div style={UI.brand}>
          <div style={UI.logoWrap}>
            <MiniLogo size={52} />
          </div>
          <div style={UI.plan}>{planLabel}</div>
        </div>

        <nav style={UI.nav} role="navigation" aria-label="Navegação principal">
          {menuPrimary.map(renderMenuButton)}

          <div style={UI.navDivider} />

          {menuTools.map(renderMenuButton)}

          <div style={UI.navDivider} />

          {menuAccount.map(renderMenuButton)}

          <button
            onClick={() => handleNavigate("__LOGOUT__")}
            title={isGuest ? "Sair do Preview" : "Sair"}
            style={UI.btn(false)}
            aria-label={isGuest ? "Sair do Preview" : "Sair"}
            type="button"
            onMouseEnter={(e) => {
              e.currentTarget.style.borderColor = "rgba(202,166,75,0.28)";
              e.currentTarget.style.transform = "translateY(-1px)";
              e.currentTarget.style.boxShadow =
                "0 10px 24px rgba(0,0,0,0.34), inset 0 0 0 1px rgba(202,166,75,0.08)";
            }}
            onMouseLeave={(e) => {
              e.currentTarget.style.borderColor = "rgba(255,255,255,0.10)";
              e.currentTarget.style.transform = "translateY(0)";
              e.currentTarget.style.boxShadow =
                "inset 0 0 0 1px rgba(255,255,255,0.015)";
            }}
          >
            <span style={UI.btnMarker(false)} />
            <span style={UI.btnHoverHint} />
            <Icon name="logout" size={22} />
          </button>

          <div style={UI.footerHint}>Palpitaco JB</div>
        </nav>
      </aside>

      <main style={UI.main}>
        <div style={UI.mobileTopBar}>
          <button
            type="button"
            style={UI.mobileMenuBtn}
            onClick={() => setSidebarOpen(true)}
            title="Menu"
            aria-label="Abrir menu"
          >
            <Icon name="menu" size={24} />
          </button>
        </div>

        <div style={UI.mainInner}>{children}</div>
      </main>
    </div>
  );
}



==============================================================================================================
ARQUIVO: .\src\pages\Dashboard\components\Sidebar\Icon.jsx
==============================================================================================================
import React from "react";

export default function Icon({ name = "home", size = 18 }) {
  const s = Math.max(14, Number(size) || 18);

  const common = {
    width: s,
    height: s,
    viewBox: "0 0 24 24",
    fill: "none",
  };

  const stroke = "rgba(255,255,255,0.88)";
  const gold = "rgba(201,168,62,0.92)";

  if (name === "home") {
    return (
      <svg {...common}>
        <path
          d="M4 10.5 12 4l8 6.5V20a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2v-9.5Z"
          stroke={stroke}
          strokeWidth="2"
          strokeLinejoin="round"
        />
        <path
          d="M9.5 22V14h5v8"
          stroke={gold}
          strokeWidth="2"
          strokeLinejoin="round"
        />
      </svg>
    );
  }

  if (name === "user") {
    return (
      <svg {...common}>
        <path
          d="M20 21a8 8 0 1 0-16 0"
          stroke={stroke}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M12 12a4 4 0 1 0-4-4 4 4 0 0 0 4 4Z"
          stroke={gold}
          strokeWidth="2"
          strokeLinejoin="round"
        />
      </svg>
    );
  }

  if (name === "calendar") {
    return (
      <svg {...common}>
        <path
          d="M7 2v3M17 2v3M3.5 9h17M5 5h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2Z"
          stroke={stroke}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M7 13h4M7 17h6"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
        />
      </svg>
    );
  }

  if (name === "trophy") {
    return (
      <svg {...common}>
        <path
          d="M8 4h8v3a4 4 0 0 1-8 0V4Z"
          stroke={stroke}
          strokeWidth="2"
          strokeLinejoin="round"
        />
        <path
          d="M6 6H4a2 2 0 0 0 2 4"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M18 6h2a2 2 0 0 1-2 4"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M12 11v4"
          stroke={stroke}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M8 21h8M10 15h4"
          stroke={stroke}
          strokeWidth="2"
          strokeLinecap="round"
        />
      </svg>
    );
  }

  if (name === "clock") {
    return (
      <svg {...common}>
        <path
          d="M12 22a10 10 0 1 0-10-10 10 10 0 0 0 10 10Z"
          stroke={stroke}
          strokeWidth="2"
        />
        <path
          d="M12 6v6l4 2"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
      </svg>
    );
  }

  if (name === "search") {
    return (
      <svg {...common}>
        <path d="M11 19a8 8 0 1 0-8-8 8 8 0 0 0 8 8Z" stroke={stroke} strokeWidth="2" />
        <path
          d="M21 21l-4.3-4.3"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
        />
      </svg>
    );
  }

  if (name === "card") {
    return (
      <svg {...common}>
        <path
          d="M4 7h16a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2Z"
          stroke={stroke}
          strokeWidth="2"
          strokeLinejoin="round"
        />
        <path d="M2 11h20" stroke={gold} strokeWidth="2" strokeLinecap="round" />
        <path d="M6 16h6" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  if (name === "download") {
    return (
      <svg {...common}>
        <path d="M12 3v10" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
        <path
          d="M8 11l4 4 4-4"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
        <path d="M4 21h16" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  if (name === "menu") {
    return (
      <svg {...common}>
        <path d="M4 7h16" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
        <path d="M4 12h16" stroke={gold} strokeWidth="2" strokeLinecap="round" />
        <path d="M4 17h16" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  if (name === "hash") {
    return (
      <svg {...common}>
        <path d="M9 3L7 21" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
        <path d="M17 3l-2 18" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
        <path d="M5 8h16" stroke={gold} strokeWidth="2" strokeLinecap="round" />
        <path d="M3 16h16" stroke={gold} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  if (name === "back") {
    return (
      <svg {...common}>
        <path
          d="M15 18l-6-6 6-6"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
        <path d="M9 12h11" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  if (name === "logout") {
    return (
      <svg {...common}>
        <path
          d="M10 17l-1 0a4 4 0 0 1-4-4V7a4 4 0 0 1 4-4h1"
          stroke={stroke}
          strokeWidth="2"
          strokeLinecap="round"
        />
        <path
          d="M15 7l5 5-5 5"
          stroke={gold}
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
        <path d="M20 12H10" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      </svg>
    );
  }

  return (
    <svg {...common}>
      <path d="M12 2v20" stroke={stroke} strokeWidth="2" strokeLinecap="round" />
      <path d="M2 12h20" stroke={gold} strokeWidth="2" strokeLinecap="round" />
    </svg>
  );
}



==============================================================================================================
ARQUIVO: .\src\pages\Dashboard\components\Sidebar\MiniLogo.jsx
==============================================================================================================
import React from "react";

export default function MiniLogo({
  size = 48,
  decorative = true,
  src = "/logo/palpitaco-jb.png",
}) {
  const s = Math.max(24, Number(size) || 48);

  return (
    <img
      src={src}
      alt={decorative ? "" : "Palpitaco JB"}
      aria-hidden={decorative ? "true" : "false"}
      width={s}
      height={s}
      draggable="false"
      style={{
        display: "block",
        width: s,
        height: s,
        objectFit: "contain",
        userSelect: "none",
        pointerEvents: "none",
        filter: "drop-shadow(0 4px 14px rgba(202,166,75,0.18))",
      }}
    />
  );
}



==============================================================================================================
ARQUIVO: .\src\pages\Dashboard\components\Sidebar\index.js
==============================================================================================================
// src/pages/Dashboard/components/Sidebar/index.js
export { default as AppShell } from "./AppShell";
export { default as Icon } from "./Icon";
export { default as MiniLogo } from "./MiniLogo";



==============================================================================================================
ARQUIVO: .\src\pages\Dashboard\dashboard.css
==============================================================================================================
/* src/pages/Dashboard/dashboard.css */

/* =========================
   DASHBOARD (Premium Frame)
   - Layout e estrutura SOMENTE do Dashboard
   - Desktop: dashboard TRAVADO (sem scroll na janela / sem scroll no main)
   - Ranking: SEM SCROLL (nem vertical nem horizontal) e sem cortar
   - Mobile: deixa crescer (overflow visível) para não cortar conteúdo
========================= */

/* ====== Base / Tokens ====== */
:root{
  --bg: #050505;

  --panel: rgba(0,0,0,0.55);
  --panel2: rgba(0,0,0,0.70);

  --border: rgba(255,255,255,0.18);
  --border2: rgba(255,255,255,0.10);

  --text: rgba(255,255,255,0.95);
  --muted: rgba(255,255,255,0.70);

  --gold: rgba(201,168,62,0.92);

  /* sombras premium */
  --shadow: 0 18px 50px rgba(0,0,0,0.55);
  --shadowSoft: 0 12px 30px rgba(0,0,0,0.45);
  --shadowTight: 0 10px 22px rgba(0,0,0,0.40);

  --radius-lg: 18px;
  --radius-md: 16px;
  --radius-sm: 12px;

  /* spacing */
  --gap: 12px;
  --gap-lg: 14px;
  --pad: 14px;

  /* Scrollbar (premium discreto) */
  --scrollTrack: rgba(255,255,255,0.06);
  --scrollThumb: rgba(255,255,255,0.18);
  --scrollThumbHover: rgba(255,255,255,0.26);

  /* altura do topo (banner + coluna direita) no desktop */
  --topH: 250px;

  /* inset opcional (se precisar compensar algo externo) */
  --dashInsetLeft: 0px;
}

/* ✅ garante box-model consistente */
*, *::before, *::after{
  box-sizing: border-box;
}

/* ====== Estrutura geral ====== */
.dashRoot{
  width: 100%;
  max-width: none;
  margin: 0;

  /* Dashboard ocupa o container do AppShell main */
  height: 100%;
  min-height: 0;

  display: grid;
  grid-template-columns: 380px minmax(0, 1fr);
  gap: var(--gap-lg);

  padding: var(--pad);
  padding-left: calc(var(--pad) + var(--dashInsetLeft));

  align-items: stretch;

  /* Desktop: sem scroll do dashboard (nem da window) */
  overflow: hidden;

  background:
    radial-gradient(900px 500px at 70% 12%, rgba(201,168,62,0.06), transparent 60%),
    radial-gradient(700px 420px at 18% 10%, rgba(255,255,255,0.035), transparent 55%);
}

/* Desktop: trava o Dashboard em “1 tela” */
@media (min-width: 981px){
  .dashRoot{
    height: calc(100dvh - (var(--pad) * 2));
    max-height: calc(100dvh - (var(--pad) * 2));
  }
}

/* ====== Helpers de Card ====== */
.dashCard{
  border: 1px solid var(--border);
  background: var(--panel2);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadowSoft);
}

.dashCardSoft{
  border: 1px solid var(--border);
  background: rgba(0,0,0,0.62);
  border-radius: var(--radius-md);
  box-shadow: var(--shadowSoft);
}

/* ====== Painel esquerdo (ranking) ====== */
.dashLeft{
  border: 1px solid var(--border);
  background: var(--panel2);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);

  overflow: hidden;
  overflow-x: hidden;

  position: sticky;
  top: var(--pad);

  max-height: calc(100dvh - (var(--pad) * 2));
  height: 100%;
  min-height: 0;

  display: flex;
  flex-direction: column;

  min-width: 0;
}

/* wrappers do ranking */
.dashLeft .tableWrap,
.dashLeft .dashTableWrap,
.dashLeft .rankingWrap{
  min-height: 0;
  overflow: hidden;
  flex: 1 1 auto;
}

/* fallback */
.dashLeft > *:last-child{
  min-height: 0;
  overflow: hidden;
  flex: 1 1 auto;
}

/* ====== Área principal ====== */
.dashMain{
  display: flex;
  flex-direction: column;
  gap: var(--gap-lg);

  min-width: 0;
  min-height: 0;

  height: 100%;
  overflow: hidden;
}

/* ====== Topo (Banner + coluna direita) ====== */
.dashTop{
  display: grid;
  grid-template-columns: minmax(0, 1fr) 520px;
  gap: var(--gap-lg);

  align-items: stretch;

  min-width: 0;
  min-height: 0;

  flex: 0 0 auto;
}

/* Banner */
.dashBanner{
  border: 1px solid var(--border);
  background: var(--panel2);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow);

  height: var(--topH);
  min-height: var(--topH);
  max-height: var(--topH);

  position: relative;
  min-width: 0;

  outline: 1px solid rgba(201,168,62,0.10);
  outline-offset: -1px;
}

.dashBanner::before{
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;

  background:
    radial-gradient(900px 320px at 28% 35%, rgba(255,255,255,0.06), transparent 60%),
    radial-gradient(700px 300px at 80% 28%, rgba(201,168,62,0.06), transparent 62%);

  z-index: 1;
}

.dashBanner::after{
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;

  box-shadow:
    inset 0 0 0 1px rgba(255,255,255,0.07),
    inset 0 0 48px rgba(0,0,0,0.22);

  z-index: 2;
}

.dashBanner img{
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 25%;
  display: block;
  filter: contrast(1.03) saturate(1.12) brightness(1.03);
}

/* Coluna direita */
.dashTopRight{
  display: flex;
  flex-direction: column;
  gap: 8px;

  height: var(--topH);
  min-height: var(--topH);
  max-height: var(--topH);

  min-width: 0;
  overflow: hidden;
  scrollbar-gutter: stable both-edges;
}

.dashTopRight > *{
  flex: 0 0 auto;
  min-width: 0;
}

/* Integração: DateRangeControl (pp_range) */
.dashTopRight .pp_range{
  min-width: 0;
  width: 100%;
}

.dashTopRight .pp_range > div:first-child{
  box-shadow: var(--shadowTight);
}

.dashTopRight .pp_range .pp_range_row{ min-width: 0; }
.dashTopRight .pp_range input[type="date"]{ min-width: 0; }
.dashTopRight .pp_range input[type="range"]{ overflow: visible; }
.dashTopRight .pp_range{ filter: none; }

/* ====== (Legado / opcional) Linha das datas/anos ====== */
.dashDateRow{
  display: grid;
  grid-template-columns: 132px 132px 1fr;
  gap: 8px;
  padding: 8px;
  align-items: center;

  border: 1px solid var(--border);
  background: rgba(0,0,0,0.62);
  border-radius: var(--radius-md);

  min-width: 0;

  box-shadow:
    0 10px 22px rgba(0,0,0,0.38),
    inset 0 0 0 1px rgba(255,255,255,0.06);
}

.dashDateBox{
  border: 1px solid var(--border);
  background: rgba(0,0,0,0.74);
  border-radius: var(--radius-sm);
  padding: 9px 10px;

  font-weight: 850;
  letter-spacing: 0.15;
  text-align: center;
  white-space: nowrap;
  font-size: 13px;

  min-width: 0;

  box-shadow: inset 0 0 0 1px rgba(255,255,255,0.05);
}

.dashYearButtons{
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
  align-content: flex-start;
  min-width: 0;
}

.dashYearBtn{
  border: 1px solid var(--border);
  background: rgba(0,0,0,0.74);
  color: var(--text);
  border-radius: var(--radius-sm);

  padding: 9px 12px;

  font-weight: 850;
  min-width: 64px;
  font-size: 13px;

  cursor: pointer;
  transition: transform .08s ease, border-color .12s ease, background .12s ease, box-shadow .12s ease;
}

.dashYearBtn:hover{
  transform: translateY(-1px);
  border-color: rgba(255,255,255,0.30);
  box-shadow: 0 10px 18px rgba(0,0,0,0.35);
}

.dashYearBtn:active{ transform: translateY(0px); }

.dashYearBtn.isActive{
  border-color: rgba(255,255,255,0.42);
  box-shadow:
    0 10px 18px rgba(0,0,0,0.35),
    0 0 0 1px rgba(201,168,62,0.16) inset,
    0 0 0 3px rgba(201,168,62,0.08);
}

/* ====== Faixa de filtros ====== */
.dashFilters{
  border: 1px solid var(--border);
  background: rgba(0,0,0,0.62);
  border-radius: var(--radius-md);

  padding: 12px;

  box-shadow:
    0 12px 26px rgba(0,0,0,0.40),
    inset 0 0 0 1px rgba(255,255,255,0.06);

  min-width: 0;
  flex: 0 0 auto;

  outline: 1px solid rgba(201,168,62,0.07);
  outline-offset: -1px;
}

/* ====== Área de gráficos ====== */
.dashCharts{
  background: transparent;
  border-radius: var(--radius-md);

  min-height: 0;
  min-width: 0;

  flex: 1 1 auto;

  overflow: hidden;
  scrollbar-gutter: stable both-edges;
}

/* Anti scroll fantasma em flex/grid (Windows/Chrome)
   ✅ NÃO sobrescrever a altura “1 tela” do desktop */
.dashMain,
.dashCharts,
.dashLeft{
  min-height: 0;
}

/* Acessibilidade */
.dashYearBtn:focus-visible{
  outline: none;
  border-color: rgba(201,168,62,0.55);
  box-shadow: 0 0 0 3px rgba(201,168,62,0.14);
}

/* Scrollbar (Dashboard scope) */
.dashLeft *::-webkit-scrollbar,
.dashMain *::-webkit-scrollbar,
.dashTopRight::-webkit-scrollbar{
  width: 10px;
  height: 10px;
}

.dashLeft *::-webkit-scrollbar-track,
.dashMain *::-webkit-scrollbar-track,
.dashTopRight::-webkit-scrollbar-track{
  background: var(--scrollTrack);
  border-radius: 999px;
}

.dashLeft *::-webkit-scrollbar-thumb,
.dashMain *::-webkit-scrollbar-thumb,
.dashTopRight::-webkit-scrollbar-thumb{
  background: var(--scrollThumb);
  border-radius: 999px;
  border: 2px solid rgba(0,0,0,0.45);
}

.dashLeft *::-webkit-scrollbar-thumb:hover,
.dashMain *::-webkit-scrollbar-thumb:hover,
.dashTopRight::-webkit-scrollbar-thumb:hover{
  background: var(--scrollThumbHover);
}

/* =========================
   TABELA RANKING — coluna imagem centralizada
========================= */
.dashLeft table thead th:first-child,
.dashLeft table tbody td:first-child{
  text-align: center;
  vertical-align: middle;
}

.dashLeft table tbody td:first-child{
  padding-left: 0 !important;
  padding-right: 0 !important;
}

.dashLeft table tbody td:first-child > *{
  margin-left: auto;
  margin-right: auto;
}

.dashLeft table tbody td:first-child img{
  display: block;
  margin: 0 auto;
  width: 38px;
  height: 38px;
  object-fit: contain;
}

/* Diminuir fonte SOMENTE da coluna ANIMAL */
.dashLeft table tbody td:nth-child(3){
  font-size: 13px;
}

/* =========================
   RANKING — PREMIUM CANÔNICO (ÚNICO)
   Objetivo:
   - card interno encosta no fundo (sem “vazio”)
   - 25 linhas ocupam exatamente a altura (sem scroll)
   - NÃO corta "Palpite" (colunas com largura fixa)
========================= */
@media (min-width: 981px){

  :root{
    --rankRows: 25;
    --rankHeadH: 44px; /* ajuste fino (40–52) se precisar */
  }

  /* Painel esquerdo e conteúdo em coluna */
  .dashLeft{
    display: flex;
    flex-direction: column;
    min-height: 0;
  }

  /* O “card interno” precisa esticar */
  .dashLeft .dashCard,
  .dashLeft .dashCardSoft{
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    flex-direction: column;

    padding-bottom: 0 !important;
    margin-bottom: 0 !important;
  }

  /* Wrapper do ranking também estica */
  .dashLeft .tableWrap,
  .dashLeft .dashTableWrap,
  .dashLeft .rankingWrap{
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    flex-direction: column;

    padding-bottom: 0 !important;
    margin-bottom: 0 !important;
  }

  /* Tabela ocupa toda a altura do card */
  .dashLeft table{
    width: 100%;
    height: 100%;
    flex: 1 1 auto;

    border-collapse: collapse;
    table-layout: fixed; /* necessário para alinhamento quando tbody vira block */
  }

  /* ✅ LARGURAS FIXAS (evita cortar "Palpite") */
  .dashLeft table thead th:nth-child(1),
  .dashLeft table tbody td:nth-child(1){ width: 44px; }  /* Img */
  .dashLeft table thead th:nth-child(2),
  .dashLeft table tbody td:nth-child(2){ width: 56px; }  /* Grupo */
  .dashLeft table thead th:nth-child(4),
  .dashLeft table tbody td:nth-child(4){ width: 70px; }  /* Apar. */
  .dashLeft table thead th:nth-child(5),
  .dashLeft table tbody td:nth-child(5){ width: 74px; }  /* Palpite */
  /* Animal (3) fica com o restante */

  /* Thead “fixo” e tbody com altura controlada */
  .dashLeft table thead{
    display: table;
    width: 100%;
    table-layout: fixed;
  }

  .dashLeft table thead th{
    height: var(--rankHeadH);
    vertical-align: middle;
    padding-top: 8px;
    padding-bottom: 8px;
  }

  .dashLeft table tbody{
    display: block;
    width: 100%;
    height: calc(100% - var(--rankHeadH));
  }

  /* Cada linha vira table para manter colunas alinhadas */
  .dashLeft table tbody tr{
    display: table;
    width: 100%;
    table-layout: fixed;

    height: calc(100% / var(--rankRows));
  }

  .dashLeft table tbody td{
    vertical-align: middle;
    padding-top: 6px;
    padding-bottom: 6px;
    line-height: 1.15;
  }
}

/* ====== Responsivo ====== */
@media (max-width: 1400px){
  .dashTop{ grid-template-columns: minmax(0, 1fr) 480px; }
}

@media (max-width: 1200px){
  :root{ --topH: 210px; }
  .dashRoot{ grid-template-columns: 340px minmax(0, 1fr); }
  .dashTop{ grid-template-columns: minmax(0, 1fr) 440px; }
}

/* breakpoint alinhado com AppShell (vw < 980) */
@media (max-width: 980px){
  :root{ --topH: 200px; }

  .dashRoot{
    grid-template-columns: 1fr;
    padding: 12px;
    padding-left: 12px;

    height: auto;
    max-height: none;
    overflow: visible;
  }

  .dashLeft{
    position: relative;
    top: auto;
    max-height: none;
    height: auto;
    overflow: hidden;
  }

  .dashLeft > *:last-child{
    overflow: visible;
  }

  .dashMain{
    height: auto;
    overflow: visible;
  }

  .dashTop{
    grid-template-columns: 1fr;
  }

  .dashBanner{
    height: var(--topH);
    min-height: var(--topH);
    max-height: var(--topH);
  }

  .dashTopRight{
    height: auto;
    min-height: 0;
    max-height: none;
    overflow: visible;
    scrollbar-gutter: auto;
  }

  .dashDateRow{
    grid-template-columns: 1fr 1fr;
    grid-auto-rows: auto;
  }

  .dashYearButtons{
    grid-column: 1 / -1;
    justify-content: flex-start;
  }

  .dashCharts{
    overflow: visible;
    scrollbar-gutter: auto;
  }
}

/* extra: telas bem pequenas */
@media (max-width: 520px){
  .dashRoot{
    padding: 10px;
    gap: 12px;
  }

  .dashFilters{
    padding: 10px;
  }
}

/* ✅ garante que o ChartsGrid estique e não comprima SVG/fonte */
.dashCharts{
  display: flex;
}
.dashCharts > *{
  flex: 1 1 auto;
  min-height: 0;
  min-width: 0;
}
/* Banner institucional Palpitaco JB */
.dashBanner--brand img{
  object-fit: contain !important;
  padding: 18px 28px;
  background:
    radial-gradient(circle at 20% 20%, rgba(202,166,75,0.18), transparent 34%),
    radial-gradient(circle at 80% 40%, rgba(202,166,75,0.10), transparent 38%),
    linear-gradient(135deg, #050505 0%, #111 52%, #050505 100%);
}



==============================================================================================================
REFERÊNCIAS A onNavigate, active E ÍCONES
==============================================================================================================

Path                                                                         LineNumber Line
----                                                                         ---------- ----
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx          2 import Icon from "./Icon";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         10   ACCOUNT: "account",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         11   RESULTS: "results",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         12   TOP3: "top3",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         13   LATE: "late",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         14   SEARCH: "search",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         16   DOWNLOADS: "downloads",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         17   CENTENAS: "centenas",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         20 const ACCOUNT_SESSION_KEY = "pp_session_v1";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         21 const LS_GUEST_ACTIVE_KEY = "pp_guest_active_v1";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         46   const raw = safeReadLS(ACCOUNT_SESSION_KEY);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx         97 export default function AppShell({ active, onNavigate, onLogout, children }) {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        119       if (safeReadLS(LS_GUEST_ACTIVE_KEY) === "1") {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        149       if (e.key === ACCOUNT_SESSION_KEY || e.key === LS_GUEST_ACTIVE_KEY) {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        169     const isDashboard = active === ROUTES.DASHBOARD;
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        188   }, [active, isMobile, sidebarOpen]);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        206     if (key === "__LOGOUT__") {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        211       safeRemoveLS(ACCOUNT_SESSION_KEY);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        212       safeRemoveLS(LS_GUEST_ACTIVE_KEY);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        214       onLogout?.();
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        218     onNavigate?.(key);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        233     const isDashboard = active === ROUTES.DASHBOARD;
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        269           transform: sidebarOpen ? "translateX(0)" : "translateX(-110%)",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        350     const btn = (isActive) => ({
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        355       border: `1px solid ${isActive ? "rgba(202,166,75,0.55)" : BORDER}`,
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        356       background: isActive
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        363       boxShadow: isActive
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        371     const btnMarker = (isActive) => ({
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        378       background: isActive ? GOLD : "transparent",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        379       boxShadow: isActive ? `0 0 12px ${GOLD_SOFT}` : "none",
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        461   }, [isMobile, sidebarOpen, active]);
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        464     { key: ROUTES.DASHBOARD, icon: "home", title: "Dashboard" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        465     { key: ROUTES.RESULTS, icon: "calendar", title: "Resultados" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        466     { key: ROUTES.TOP3, icon: "trophy", title: "Top 3" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        467     { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        471     { key: ROUTES.SEARCH, icon: "search", title: "Busca" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        472     { key: ROUTES.CENTENAS, icon: "hash", title: "Centenas" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        473     { key: ROUTES.DOWNLOADS, icon: "download", title: "Downloads" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        476   const menuAccount = [
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        477     { key: ROUTES.ACCOUNT, icon: "user", title: "Minha Conta" },
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        481     const isActive = active === item.key;
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        488         style={UI.btn(isActive)}
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        489         aria-current={isActive ? "page" : undefined}
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        493           if (!isActive) {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        495             e.currentTarget.style.transform = "translateY(-1px)";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        501           if (!isActive) {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        503             e.currentTarget.style.transform = "translateY(0)";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        509         <span style={UI.btnMarker(isActive)} />
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        511         <Icon name={item.icon} size={22} />
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        524       <aside style={UI.sidebar} aria-label="Menu lateral">
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        541           {menuAccount.map(renderMenuButton)}
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        544             onClick={() => handleNavigate("__LOGOUT__")}
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        551               e.currentTarget.style.transform = "translateY(-1px)";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        557               e.currentTarget.style.transform = "translateY(0)";
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        564             <Icon name="logout" size={22} />
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\AppShell.jsx        580             <Icon name="menu" size={24} />
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\Icon.jsx              3 export default function Icon({ name = "home", size = 18 }) {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\Icon.jsx            129   if (name === "search") {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\Icon.jsx            210   if (name === "logout") {
C:\Users\glaub\palpitaco\src\pages\Dashboard\components\Sidebar\index.js              3 export { default as Icon } from "./Icon";


