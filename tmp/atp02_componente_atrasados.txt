===== ATP-02 =====

01. src/pages/Top3/top3.engine.before-history-stats-20260710_111832.js
score=218
  00008:   toHourBucket,
  00035:   const targetH = toHourBucket(currentHour);
  00045:       const h = toHourBucket(pickDrawHour(draw));
  00071:   const h = toHourBucket(pickDrawHour(draw));
  00195: function ymdHourToTs(ymd, hourBucket) {
  00197:   const h = toHourBucket(hourBucket);
  00233:   lotteryKey,
  00239:   const key = safeStr(lotteryKey).toUpperCase();
  00253:   return new Set((Array.isArray(schedule) ? schedule : []).map(toHourBucket));
  00258:   return s.has(toHourBucket(hhmm));
  00268:       const ta = ymdHourToTs(pickDrawYMD(a), toHourBucket(pickDrawHour(a)));
  00269:       const tb = ymdHourToTs(pickDrawYMD(b), toHourBucket(pickDrawHour(b)));
  00278:   targetHourBucket,
  00284:   const t = hourToInt(targetHourBucket);
  00289:     .map((d) => ({ d, h: hourToInt(toHourBucket(pickDrawHour(d))) }))

02. src/pages/Top3/top3.engine.before-history-stats-20260710_112326.js
score=218
  00008:   toHourBucket,
  00035:   const targetH = toHourBucket(currentHour);
  00045:       const h = toHourBucket(pickDrawHour(draw));
  00071:   const h = toHourBucket(pickDrawHour(draw));
  00195: function ymdHourToTs(ymd, hourBucket) {
  00197:   const h = toHourBucket(hourBucket);
  00233:   lotteryKey,
  00239:   const key = safeStr(lotteryKey).toUpperCase();
  00253:   return new Set((Array.isArray(schedule) ? schedule : []).map(toHourBucket));
  00258:   return s.has(toHourBucket(hhmm));
  00268:       const ta = ymdHourToTs(pickDrawYMD(a), toHourBucket(pickDrawHour(a)));
  00269:       const tb = ymdHourToTs(pickDrawYMD(b), toHourBucket(pickDrawHour(b)));
  00278:   targetHourBucket,
  00284:   const t = hourToInt(targetHourBucket);
  00289:     .map((d) => ({ d, h: hourToInt(toHourBucket(pickDrawHour(d))) }))

03. src/pages/Top3/top3.engine.js
score=217
  00008:   toHourBucket,
  00035:   const targetH = toHourBucket(currentHour);
  00045:       const h = toHourBucket(pickDrawHour(draw));
  00071:   const h = toHourBucket(pickDrawHour(draw));
  00195: function ymdHourToTs(ymd, hourBucket) {
  00197:   const h = toHourBucket(hourBucket);
  00233:   lotteryKey,
  00239:   const key = safeStr(lotteryKey).toUpperCase();
  00253:   return new Set((Array.isArray(schedule) ? schedule : []).map(toHourBucket));
  00258:   return s.has(toHourBucket(hhmm));
  00268:       const ta = ymdHourToTs(pickDrawYMD(a), toHourBucket(pickDrawHour(a)));
  00269:       const tb = ymdHourToTs(pickDrawYMD(b), toHourBucket(pickDrawHour(b)));
  00278:   targetHourBucket,
  00284:   const t = hourToInt(targetHourBucket);
  00289:     .map((d) => ({ d, h: hourToInt(toHourBucket(pickDrawHour(d))) }))

04. src/pages/Late/Late.jsx
score=119
  00001: // src/pages/Late/Late.jsx
  00001: // src/pages/Late/Late.jsx
  00006:   getKingLateByRange,
  00006:   getKingLateByRange,
  00014:  * Late (Atrasados) — Premium
  00014:  * Late (Atrasados) — Premium
  00014:  * Late (Atrasados) — Premium
  00014:  * Late (Atrasados) — Premium
  00017:  * - Usa getKingLateByRange() (varre histórico interno com prizes)
  00017:  * - Usa getKingLateByRange() (varre histórico interno com prizes)
  00120: function toHourBucket(value) {
  00145: function pickLatestCloseHour(drawsLike) {
  00145: function pickLatestCloseHour(drawsLike) {
  00180: function mergeLateLists(lists, baseYmd) {
  00180: function mergeLateLists(lists, baseYmd) {

05. src/pages/Top3/top3.hooks.js
score=106
  00010:   toHourBucket,
  00087:   const h = toHourBucket(pickDrawHour(draw));
  00103:   const h = toHourBucket(pickDrawHour(draw));
  00107: function hasDrawAtHour(draws, hourBucket) {
  00108:   const target = toHourBucket(hourBucket);
  00112:     const h = toHourBucket(pickDrawHour(d));
  00137: function isDrawValidForLotterySchedule(draw, lotteryKey) {
  00139:   const h = toHourBucket(pickDrawHour(draw));
  00144:     lotteryKey,
  00152:     ? schedule.map(toHourBucket).filter(Boolean)
  00158: function findLatestHistoricalBaseDraw({
  00158: function findLatestHistoricalBaseDraw({
  00160:   lotteryKey,
  00162:   targetHourBucket,
  00165:   const targetHour = toHourBucket(targetHourBucket);

06. src/pages/Top3/Top3View.jsx
score=91
  00099: function hourBucketToSortValue(hour) {
  00733:           gridTemplateColumns: isHero ? "300px minmax(0, 1fr)" : "1fr",
  00733:           gridTemplateColumns: isHero ? "300px minmax(0, 1fr)" : "1fr",
  00816:                   gridTemplateColumns: "1fr 1fr",
  00816:                   gridTemplateColumns: "1fr 1fr",
  00846:               gridTemplateColumns: isHero
  00846:               gridTemplateColumns: isHero
  00949:             gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
  00949:             gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
  01232:     lotteryKeySafe,
  01233:     setLotteryKey,
  01239:     analysisHourBucket,
  01242:     lastHourBucket,
  01275:     return formatYmdHour(analysisYmd, analysisHourBucket);
  01276:   }, [analysisYmd, analysisHourBucket]);

07. src/pages/Centenas/CentenasView.jsx
score=77
  00034:  * - Agora: se draw.prizes não existir, agrupamos prizes flat por (ymd + hourBucket).
  00115: function toHourBucketHH00(value) {
  00481:         label: "Todas as loterias",
  00482:         lotteryKeys: [
  00490:         lotteryKeys: [
  00497:         lotteryKeys: [
  00589:   const selectedLotteryKeys = useMemo(
  00592:         selectedLottery?.lotteryKeys
  00594:         ? selectedLottery.lotteryKeys
  00599:   const selectedLotteryKeysKey =
  00600:     selectedLotteryKeys.join(",");
  00690:       selectedLotteryKeys.includes(
  00698:       selectedLotteryKeys.includes(
  00717:   }, [selectedLotteryKeys]);
  00784:             selectedLotteryKeys.map(

08. src/pages/Top3/Top3View.premium.backup.jsx
score=73
  00073: function hourBucketToSortValue(hour) {
  00644:             gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
  00644:             gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
  00968:     lotteryKeySafe,
  00969:     setLotteryKey,
  00974:     analysisHourBucket,
  00975:     lastHourBucket,
  00983:     String(slot?.targetHour || "").trim() === String(analysisHourBucket || "").trim()
  00985: }, [timelineTop3, analysisYmd, analysisHourBucket]);
  01010:     return formatYmdHour(analysisYmd, analysisHourBucket);
  01011:   }, [analysisYmd, analysisHourBucket]);
  01014:     const from = String(lastHourBucket || "").trim();
  01015:     const to = String(analysisHourBucket || "").trim();
  01018:   }, [lastHourBucket, analysisHourBucket]);
  01103:   const curLot = String(lotteryKeySafe || "PT_RIO").toUpperCase();

09. src/services/kingResultsService.js
score=47
  00049: // ✅ cache específico para ATRASADOS (Late)
  00049: // ✅ cache específico para ATRASADOS (Late)
  00049: // ✅ cache específico para ATRASADOS (Late)
  00049: // ✅ cache específico para ATRASADOS (Late)
  00050: const LATE_CACHE = new Map(); // key -> { ts, data: lateRows[] }
  00050: const LATE_CACHE = new Map(); // key -> { ts, data: lateRows[] }
  00129: function resolveLotteryKeyForQuery(ufInput) {
  00235: function toHourBucket(value) {
  00259:    - bucket SOMENTE via closeHourBucket
  00264: function resolveHourFilter({ closeHour = null, closeHourBucket = null }) {
  00265:   const bucket = toHourBucket(closeHourBucket);
  00286:     return toHourBucket(norm) === toHourBucket(hourFilter.bucket);
  00557:   const lotteryKeyRaw = d.lottery_key ?? d.lotteryKey ?? d.lottery ?? null;
  00581:     lottery_key: lotteryKeyRaw,
  00688:     const lotteryKey =

10. src/pages/Dashboard/components/ChartsGrid.jsx
score=25
  00116: function getDrawLotteryKey(draw) {
  00120:       draw.lotteryKey ??
  00260:     const lot = getDrawLotteryKey(d) || "";
  01477:     gridTemplateColumns: "4.2fr 4.2fr 2.8fr",
  01477:     gridTemplateColumns: "4.2fr 4.2fr 2.8fr",
  01478:     gridTemplateRows: "minmax(0, 1fr) minmax(0, 1fr)",
  01478:     gridTemplateRows: "minmax(0, 1fr) minmax(0, 1fr)",
  01479:     gridTemplateAreas: `
  01479:     gridTemplateAreas: `
  01625:     gridTemplateColumns: "170px 1fr 92px",
  01625:     gridTemplateColumns: "170px 1fr 92px",
  01743:     gridTemplateColumns: "64px 1fr 84px",
  01743:     gridTemplateColumns: "64px 1fr 84px",
  01756:     gridTemplateColumns: "1fr 1fr 84px",
  01756:     gridTemplateColumns: "1fr 1fr 84px",

11. src/pages/Dashboard/components/DateRangeControl.jsx
score=22
  00394:       gridTemplateColumns: "1fr 1fr",
  00394:       gridTemplateColumns: "1fr 1fr",
  00430:       transform: "translateY(-50%)",
  00430:       transform: "translateY(-50%)",
  00454:       isolation: "isolate", // ✅ stacking context consistente pro z-index
  00454:       isolation: "isolate", // ✅ stacking context consistente pro z-index
  00463:       transform: "translateY(-50%)",
  00463:       transform: "translateY(-50%)",
  00476:       transform: "translateY(-50%)",
  00476:       transform: "translateY(-50%)",
  00551:         transform: translateY(-50%) scale(1.03);
  00551:         transform: translateY(-50%) scale(1.03);
  00555:         transform: translateY(-50%) scale(0.98);
  00555:         transform: translateY(-50%) scale(0.98);
  00562:         transform: translateY(-1px);

12. src/App.jsx
score=19
  00008: import LateMod from "./pages/Late/Late";
  00008: import LateMod from "./pages/Late/Late";
  00055:   LATE: "late",
  00055:   LATE: "late",
  00235: function loteriaToLotteryKey(loteria) {
  00373:     case ROUTES.LATE:
  00373:     case ROUTES.LATE:
  00374:       return "/late";
  00374:       return "/late";
  00396:   if (p === "/late") return ROUTES.LATE;
  00396:   if (p === "/late") return ROUTES.LATE;
  00448:   const Late = useMemo(() => resolveComponent(LateMod, "Late"), []);
  00448:   const Late = useMemo(() => resolveComponent(LateMod, "Late"), []);
  00662:       case ROUTES.LATE:
  00662:       case ROUTES.LATE:

13. src/pages/Results/Results.jsx
score=19
  00771:         closeHourBucket: null,
  00952:         grid-template-rows: auto 1fr;
  00952:         grid-template-rows: auto 1fr;
  01048:         grid-template-rows: auto auto 1fr;
  01048:         grid-template-rows: auto auto 1fr;
  01073:         grid-template-columns: repeat(2, minmax(0, 460px));
  01073:         grid-template-columns: repeat(2, minmax(0, 460px));
  01161:         grid-template-columns: 58px 1fr 110px;
  01161:         grid-template-columns: 58px 1fr 110px;
  01349:         grid-template-columns: repeat(7, 1fr);
  01349:         grid-template-columns: repeat(7, 1fr);
  01364:         grid-template-columns: repeat(7, 1fr);
  01364:         grid-template-columns: repeat(7, 1fr);
  01420:         transform:translateX(-50%);
  01420:         transform:translateX(-50%);

14. src/pages/Centenas/centenas.styles.js
score=18
  00022:         grid-template-columns: 1fr;
  00022:         grid-template-columns: 1fr;
  00025:       @media (min-width: 720px){ .cx0_filters{ grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  00025:       @media (min-width: 720px){ .cx0_filters{ grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  00026:       @media (min-width: 1100px){ .cx0_filters{ grid-template-columns: repeat(6, minmax(0, 1fr)); } }
  00026:       @media (min-width: 1100px){ .cx0_filters{ grid-template-columns: repeat(6, minmax(0, 1fr)); } }
  00071:         transform:translateY(-50%);
  00071:         transform:translateY(-50%);
  00155:         grid-template-columns: 1fr;
  00155:         grid-template-columns: 1fr;
  00160:       @media (min-width: 980px){ .cx0_grid{ grid-template-columns: 320px 1fr; } }
  00160:       @media (min-width: 980px){ .cx0_grid{ grid-template-columns: 320px 1fr; } }
  00191:         transform:translateZ(0);
  00191:         transform:translateZ(0);
  00269:         grid-template-columns: 90px 140px 140px 1fr;

15. src/pages/Dashboard/Dashboard.jsx
score=18
  00181: function normalizeHourBucket(h) {
  00252:     d.closeHourBucket ??
  00439:       <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}>
  00439:       <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}>
  00560:   const bucket = normalizeHourBucket(s);
  00586:     d.closeHourBucket ??
  00594:   const uf = String(ufKey ?? d.uf ?? d.lottery_key ?? d.lotteryKey ?? "")
  00766:     () => normalizeLoteriaKey(filters?.lotteryKey || filters?.loteria || filters?.uf),
  00767:     [filters?.lotteryKey, filters?.loteria, filters?.uf]
  00772:   const fedBucket = isFederal ? normalizeHourBucket(filters?.horario) : null;
  01107:     closeHourBucket: null,
  01187:   const closeHourBucketLocal = useMemo(() => normalizeHourBucket(filters.horario), [filters.horario]);
  01237:       if (name === "loteria" || name === "lotteryKey" || name === "uf") {
  01281:     const wantBucket = closeHourBucketLocal;
  01312:           const b = normalizeHourBucket(getDrawCloseHour(d));

16. src/pages/Dashboard/components/Sidebar/AppShell.jsx
score=18
  00013:   LATE: "late",
  00013:   LATE: "late",
  00269:           transform: sidebarOpen ? "translateX(0)" : "translateX(-110%)",
  00269:           transform: sidebarOpen ? "translateX(0)" : "translateX(-110%)",
  00467:     { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
  00467:     { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
  00467:     { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
  00467:     { key: ROUTES.LATE, icon: "clock", title: "Atrasados" },
  00495:             e.currentTarget.style.transform = "translateY(-1px)";
  00495:             e.currentTarget.style.transform = "translateY(-1px)";
  00503:             e.currentTarget.style.transform = "translateY(0)";
  00503:             e.currentTarget.style.transform = "translateY(0)";
  00524:       <aside style={UI.sidebar} aria-label="Menu lateral">
  00524:       <aside style={UI.sidebar} aria-label="Menu lateral">
  00551:               e.currentTarget.style.transform = "translateY(-1px)";

17. src/pages/Top3/top3.chain.js
score=18
  00007: import { isYMD, toHourBucket, hourToInt } from "./top3.formatters";
  00013: - Entende targetYmd / targetHourBucket
  00027:   const h = toHourBucket(pickDrawHour(draw));
  00040: function slotTs(ymd, hourBucket) {
  00042:   const h = toHourBucket(hourBucket);
  00061:     const h = toHourBucket(pickDrawHour(d));
  00083:   const from = toHourBucket(fromHour);
  00084:   const to = toHourBucket(toHour);
  00096: function getHistoryBeforeTarget(draws, targetYmd, targetHourBucket) {
  00098:   const target = slotTs(targetYmd, targetHourBucket);
  00119:     const fromHour = toHourBucket(pickDrawHour(fromDraw));
  00120:     const toHour = toHourBucket(pickDrawHour(toDraw));
  00141: function findPreviousDrawForTarget(draws, targetYmd, targetHourBucket) {
  00145:   const target = slotTs(targetYmd, targetHourBucket);
  00217:       basedOnHour: toHourBucket(pickDrawHour(lastDraw)),

18. src/pages/Downloads/Downloads.jsx
score=15
  00058: function toHourBucket(hhmm) {
  00670:       const hour = fHour === "ALL" ? null : toHourBucket(fHour);
  00678:         if (hour && toHourBucket(r.hour) !== hour) return false;
  00695:       const hour = toHourBucket(pickDrawHour(d));
  00744:     if (fHour !== "ALL") f.push(`Horário: ${toHourBucket(fHour)}`);
  00972:           grid-template-columns:
  00972:           grid-template-columns:
  00989:           .pp-exportBar{ grid-template-columns: repeat(3, minmax(0, 1fr)); }
  00989:           .pp-exportBar{ grid-template-columns: repeat(3, minmax(0, 1fr)); }
  00992:           .pp-exportBar{ grid-template-columns: repeat(2, minmax(0, 1fr)); }
  00992:           .pp-exportBar{ grid-template-columns: repeat(2, minmax(0, 1fr)); }
  00995:           .pp-exportBar{ grid-template-columns: 1fr; }
  00995:           .pp-exportBar{ grid-template-columns: 1fr; }
  01090:         .pp-btn:active{ transform: translateY(1px); }
  01090:         .pp-btn:active{ transform: translateY(1px); }

19. src/pages/Search/Search.jsx
score=15
  00100: function toHourBucket(value) {
  00587:   const selectedCloseHourBucket = useMemo(() => {
  00589:     return toHourBucket(horario);
  00677:           closeHourBucket: null,
  00678:           closeHour: selectedCloseHourBucket ? `${String(selectedCloseHourBucket).slice(0, 2)}:00` : null,
  00693:           const closeBucket = toHourBucket(closeRaw) || toHourBucket(d?.close_hour_bucket) || null;
  00696:           if (selectedCloseHourBucket) {
  00697:             const b = closeBucket || toHourBucket(closeRaw);
  00698:             if (b !== selectedCloseHourBucket) continue;
  00757:             const closeHourDisplay = closeBucket || toHourBucket(closeRaw) || closeRaw || "";
  00813:     selectedCloseHourBucket,
  01005:           transform:translateY(-1px);
  01005:           transform:translateY(-1px);
  01061:         .ppBtn:hover{ transform:translateY(-1px); border-color:rgba(202,166,75,0.55); background:rgba(0,0,0,0.72); }
  01061:         .ppBtn:hover{ transform:translateY(-1px); border-color:rgba(202,166,75,0.55); background:rgba(0,0,0,0.72); }

20. src/pages/Top3/__tests__/top3.backtest.layers.test.js
score=15
  00018: import { toHourBucket, hourToInt, addDaysYMD } from "../top3.formatters";
  00029:   const h = toHourBucket(pickDrawHour(draw));
  00041:   const h = toHourBucket(pickDrawHour(draw));
  00088:   const computedNextHour = toHourBucket(computed?.meta?.next?.hour || "");
  00091:   const expectedHour = toHourBucket(found?.hour || nextSlot?.hour || "");
  00097:         `Último sorteio: ${pickDrawYMD(drawLast)} ${toHourBucket(pickDrawHour(drawLast))}`,
  00113:         `Último sorteio: ${pickDrawYMD(drawLast)} ${toHourBucket(pickDrawHour(drawLast))}`,
  00114:         `Próximo sorteio: ${pickDrawYMD(nextDraw)} ${toHourBucket(pickDrawHour(nextDraw))}`,
  00187:     const lastH = toHourBucket(pickDrawHour(drawLast));
  00191:       lotteryKey: "PT_RIO",
  00193:       hourBucket: lastH,
  00202:       lotteryKey: "PT_RIO",
  00221:       lotteryKey: "PT_RIO",
  00263:     const prevHour = toHourBucket(computed?.meta?.trigger?.hour || "") || "";
  00264:     const nextHour = toHourBucket(computed?.meta?.next?.hour || "") || "";


RELATÓRIO: C:\Users\glaub\palpitaco\tmp\atp02_componente_atrasados.txt
