PATCH_APLICADO
diff --git a/src/pages/Late/Late.jsx b/src/pages/Late/Late.jsx
index 3381e51..b38659f 100644
--- a/src/pages/Late/Late.jsx
+++ b/src/pages/Late/Late.jsx
@@ -499,29 +499,51 @@ export default function Late() {
     return { minYmd, maxYmd };
   }
 
-  function getEffectiveTargetYmd(targetYmd, effBounds) {
+  function getLateDateContext(targetYmd, effBounds) {
     const minYmd = effBounds?.minYmd || null;
-    const maxYmd = effBounds?.maxYmd || null;
-
-    if (!minYmd || !maxYmd || !isYMD(targetYmd)) {
-      return { effective: targetYmd, note: "" };
-    }
-
-    if (targetYmd < minYmd)
-      return { effective: minYmd, note: `Data fora do bounds. Usando ${ymdToBR(minYmd)}.` };
-    if (targetYmd > maxYmd)
-      return { effective: maxYmd, note: `Data fora do bounds. Usando ${ymdToBR(maxYmd)}.` };
-
-    return { effective: targetYmd, note: "" };
+    const maxImportedYmd = effBounds?.maxYmd || null;
+    const todayYmd = todayYMDLocal();
+
+    const referenceYmd = normalizeSingleDateWithBounds(
+      targetYmd,
+      minYmd,
+      todayYmd
+    );
+
+    const queryToYmd =
+      isYMD(maxImportedYmd) &&
+      isYMD(referenceYmd) &&
+      referenceYmd > maxImportedYmd
+        ? maxImportedYmd
+        : referenceYmd;
+
+    const note =
+      isYMD(targetYmd) && referenceYmd !== targetYmd
+        ? `Data futura ou fora do hist├│rico. Usando ${ymdToBR(referenceYmd)}.`
+        : "";
+
+    return {
+      referenceYmd,
+      queryToYmd,
+      note,
+    };
   }
 
   async function buildLateList({ targetYmd, effBounds }) {
-    const { effective: safeTarget, note } = getEffectiveTargetYmd(targetYmd, effBounds);
+    const {
+      referenceYmd,
+      queryToYmd,
+      note,
+    } = getLateDateContext(targetYmd, effBounds);
+
     if (note) setError(note);
 
     const minYmd = effBounds?.minYmd || null;
-    if (!minYmd) {
-      return { rows: [], meta: { effectiveTargetYmd: safeTarget } };
+    if (!minYmd || !queryToYmd) {
+      return {
+        rows: [],
+        meta: { effectiveTargetYmd: referenceYmd },
+      };
     }
 
     // 1) Consulta atrasados por posi├º├úo (1 ou v├írias) via SUA base
@@ -531,8 +553,8 @@ export default function Late() {
         uf: selectedLateUf,
         lotteries: selectedLateLotteries,
         dateFrom: minYmd,
-        dateTo: safeTarget,
-        baseDate: safeTarget,
+        dateTo: queryToYmd,
+        baseDate: referenceYmd,
         prizePosition: pos,
 
         closeHour: null,
@@ -551,7 +573,7 @@ export default function Late() {
     }
 
     // 2) Merge (1-5) e c├ílculo de daysLate
-    const merged = mergeLateLists(lists, safeTarget);
+    const merged = mergeLateLists(lists, referenceYmd);
 
     // 3) Injeta label/imagem
     const out = merged.map((r) => {
@@ -588,7 +610,7 @@ export default function Late() {
 
     const finalRows = out.map((r, idx) => ({ ...r, pos: idx + 1 }));
 
-    return { rows: finalRows, meta: { effectiveTargetYmd: safeTarget } };
+    return { rows: finalRows, meta: { effectiveTargetYmd: referenceYmd } };
   }
 
   const refresh = useCallback(
@@ -662,7 +684,11 @@ export default function Late() {
     const maxYmd = bounds?.maxYmd;
     if (!isYMD(minYmd) || !isYMD(maxYmd)) return;
 
-    const normalized = normalizeSingleDateWithBounds(dateYmd, minYmd, maxYmd);
+    const normalized = normalizeSingleDateWithBounds(
+      dateYmd,
+      minYmd,
+      todayYMDLocal()
+    );
     if (normalized && normalized !== dateYmd) setDateYmd(normalized);
   }, [bounds?.minYmd, bounds?.maxYmd, dateYmd]);
 
@@ -743,7 +769,11 @@ export default function Late() {
     (raw) => {
       const minYmd = bounds?.minYmd;
       const maxYmd = bounds?.maxYmd;
-      const next = normalizeSingleDateWithBounds(String(raw || ""), minYmd, maxYmd);
+      const next = normalizeSingleDateWithBounds(
+        String(raw || ""),
+        minYmd,
+        todayYMDLocal()
+      );
       setDateYmd(next);
     },
     [bounds?.minYmd, bounds?.maxYmd]
@@ -994,7 +1024,7 @@ LATE_LOTTERY_OPTIONS.find(
               type="date"
               value={dateYmd}
               min={bounds?.minYmd || undefined}
-              max={bounds?.maxYmd || undefined}
+              max={todayYMDLocal()}
               onChange={(e) => onChangeDate(e.target.value || todayYMDLocal())}
             />
           </div>

> palpitaco@0.1.0 build
> react-scripts build

Creating an optimized production build...
Compiled with warnings.

[eslint] 
src\pages\Account\Account.jsx
  Line 1:1:  Unexpected Unicode BOM (Byte Order Mark)  unicode-bom

src\pages\Account\account.profile.service.js
  Line 322:11:  'lastActiveAt' is assigned a value but never used  no-unused-vars

src\pages\Centenas\CentenasView.jsx
  Line 82:10:   'todayYMDLocal' is defined but never used  no-unused-vars
  Line 419:10:  'digitFromKey' is defined but never used   no-unused-vars

src\pages\Dashboard\Dashboard.jsx
  Line 30:10:  'loadDashboardStatistics' is defined but never used  no-unused-vars

src\pages\Late\Late.jsx
  Line 645:5:   React Hook useCallback has missing dependencies: 'buildLateList' and 'refreshBoundsAndLastDraw'. Either include them or remove the dependency array  react-hooks/exhaustive-deps
  Line 771:13:  'maxYmd' is assigned a value but never used                                                                                                          no-unused-vars

src\pages\Top3\modules\scoreEngine\contextEvidence.js
  Line 1:1:  'use strict' is unnecessary inside of modules  strict

src\pages\Top3\modules\scoreEngine\evidenceEngine.js
  Line 1:1:  'use strict' is unnecessary inside of modules  strict

src\pages\Top3\modules\scoreEngine\frequencyEvidence.js
  Line 1:1:  'use strict' is unnecessary inside of modules  strict

src\pages\Top3\modules\scoreEngine\scoreConfig.js
  Line 1:1:  'use strict' is unnecessary inside of modules  strict

src\pages\Top3\modules\scoreEngine\scoreEngineV2.js
  Line 1:1:  'use strict' is unnecessary inside of modules  strict

src\pages\Top3\top3.engine.js
  Line 220:7:  'FEDERAL_20_REMOVES_PT_RIO_18_START_YMD' is assigned a value but never used  no-unused-vars

src\pages\Top3\top3.hooks.js
  Line 107:10:  'hasDrawAtHour' is defined but never used                                                                                                       no-unused-vars
  Line 843:6:   React Hook useCallback has a missing dependency: 'debugTop3'. Either include it or remove the dependency array                                  react-hooks/exhaustive-deps
  Line 853:6:   React Hook useEffect has missing dependencies: 'lookback', 'lotteryKeySafe', and 'ymdSafe'. Either include them or remove the dependency array  react-hooks/exhaustive-deps

Search for the keywords to learn more about each warning.
To ignore, add // eslint-disable-next-line to the line before.

File sizes after gzip:

  343.56 kB (+25 B)  build\static\js\main.49537b23.js
  138.8 kB           build\static\js\238.133659d3.chunk.js
  128.07 kB          build\static\js\389.21aa2d35.chunk.js
  46.35 kB           build\static\js\239.6b4eaf90.chunk.js
  42.9 kB            build\static\js\455.7220bbdf.chunk.js
  9.79 kB            build\static\js\857.e51a2e06.chunk.js
  8.69 kB            build\static\js\977.efeedf50.chunk.js
  6.2 kB             build\static\css\main.64e2afab.css

The project was built assuming it is hosted at /.
You can control this with the homepage field in your package.json.

The build folder is ready to be deployed.
You may serve it with a static server:

  serve -s build

Find out more about deployment here:

  https://cra.link/deployment

[main db6438a] fix(late): separar data-base do ultimo sorteio
 1 file changed, 53 insertions(+), 23 deletions(-)
To https://github.com/glauberseguros-commits/palpitacojb.git
   8a50b30..db6438a  main -> main
