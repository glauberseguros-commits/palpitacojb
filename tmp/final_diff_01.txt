====================================================================================================
FINAL-DIFF-01 — DIFERENÇAS PENDENTES
====================================================================================================
Gerado em: 25/07/2026 12:54:19
====================================================================================================

====================================================================================================
ARQUIVO: .\backend\scripts\autoImportToday.js
====================================================================================================
diff --git a/backend/scripts/autoImportToday.js b/backend/scripts/autoImportToday.js
index 46a845f..d13317a 100644
--- a/backend/scripts/autoImportToday.js
+++ b/backend/scripts/autoImportToday.js
@@ -1,4 +1,4 @@
-"use strict";
+´╗┐"use strict";
 
 const fs = require("fs");
 const path = require("path");
@@ -1603,4 +1603,4 @@ main().catch((e) => {
   logLine(`ERRO: ${e?.message || e}`, "ERROR");
   releaseLock();
   process.exit(1);
-});
+});
\ No newline at end of file


====================================================================================================
ARQUIVO: .\src\pages\Top3\modules\scoreEngine\scoreEngineV2.js
====================================================================================================
diff --git a/src/pages/Top3/modules/scoreEngine/scoreEngineV2.js b/src/pages/Top3/modules/scoreEngine/scoreEngineV2.js
index 95961c5..46e7da5 100644
--- a/src/pages/Top3/modules/scoreEngine/scoreEngineV2.js
+++ b/src/pages/Top3/modules/scoreEngine/scoreEngineV2.js
@@ -3,6 +3,78 @@
 import scoreConfig from "./scoreConfig";
 import { collectEvidence } from "./evidenceEngine";
 
+
+/* TPP01_ABLATION_SUPPORT_START */
+function getTop3AblationVariant() {
+  if (
+    typeof global === "undefined" ||
+    !global.__TOP3_ABLATION__
+  ) {
+    return "A00";
+  }
+
+  return String(
+    global.__TOP3_ABLATION__
+  )
+    .trim()
+    .toUpperCase();
+}
+
+function resolveScoreConfigForAblation() {
+  const variant =
+    getTop3AblationVariant();
+
+  if (
+    variant === "A00" ||
+    variant === "BASELINE" ||
+    variant === "BASELINE_COMPLETO"
+  ) {
+    return scoreConfig;
+  }
+
+  const evidenceModules = {
+    ...(scoreConfig?.evidenceModules || {}),
+  };
+
+  for (
+    const [name, config]
+    of Object.entries(evidenceModules)
+  ) {
+    evidenceModules[name] = {
+      ...(config || {}),
+    };
+  }
+
+  if (
+    variant === "A01" ||
+    variant === "SEM_FREQUENCY"
+  ) {
+    evidenceModules.frequency = {
+      ...(evidenceModules.frequency || {}),
+      enabled: false,
+    };
+  } else if (
+    variant === "A02" ||
+    variant === "SEM_CONTEXT"
+  ) {
+    evidenceModules.context = {
+      ...(evidenceModules.context || {}),
+      enabled: false,
+    };
+  } else {
+    throw new Error(
+      `Variante de abla├º├úo desconhecida: ${variant}`
+    );
+  }
+
+  return {
+    ...scoreConfig,
+    evidenceModules,
+  };
+}
+/* TPP01_ABLATION_SUPPORT_END */
+
+
 /**
  * Score Engine V2 inicial
  *
@@ -54,7 +126,7 @@ function scoreItem(item = {}, context = {}) {
   const collected = collectEvidence({
     item,
     context,
-    config: scoreConfig,
+    config: resolveScoreConfigForAblation(),
   });
 
   const strength = calculateEvidenceStrength(collected.evidence);


====================================================================================================
ARQUIVO: .\src\shared\predictiveMilharEngine\index.js
====================================================================================================
diff --git a/src/shared/predictiveMilharEngine/index.js b/src/shared/predictiveMilharEngine/index.js
index ced9cc3..2930f21 100644
--- a/src/shared/predictiveMilharEngine/index.js
+++ b/src/shared/predictiveMilharEngine/index.js
@@ -9,7 +9,7 @@
  * - n├úo altera resultados;
  * - n├úo altera o motor preditivo de grupos do Top3.
  *
- * O m├│dulo preserva temporariamente o comportamento do motor V2
+ * O m├│dulo preserva temporariamente os comportamentos dos motores V2 e V3
  * enquanto os consumidores s├úo migrados de forma independente.
  */
 
@@ -25,5 +25,10 @@ export {
   buildMilharAudit,
 } from "../../pages/Centenas/modules/milharProbabilityEngine";
 
+export {
+  buildMilharRecommendationV3,
+  diversifyMilharRecommendationsV3,
+} from "../../pages/Centenas/modules/milharProbabilityEngineV3";
+
 export const PREDICTIVE_MILHAR_ENGINE_CONTRACT =
   "PREDICTIVE_MILHAR_ENGINE_SHARED_V1";


